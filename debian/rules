#!/usr/bin/make -f

%:
	dh ${@} --parallel --with=systemd

override_dh_systemd_enable:
	dh_systemd_enable --package=desktop-defaults --name=fll-desktop

override_dh_systemd_start:
	dh_systemd_start --package=desktop-defaults --no-start fll-desktop.service
