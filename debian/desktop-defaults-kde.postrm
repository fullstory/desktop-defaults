#!/bin/sh

set -e

case "$1" in
	remove)
		dpkg-divert --remove --package desktop-defaults-kde --rename \
			--divert /usr/bin/kdesktop_lock.real /usr/bin/kdesktop_lock
		;;
esac

#DEBHELPER#

exit 0
